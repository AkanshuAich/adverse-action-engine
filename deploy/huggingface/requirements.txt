# Spaces installs from this file. The console needs the core package and the
# ml extras (the review screen renders SHAP factors from the audit payload,
# and the package imports pandas at module scope); it does NOT need the ai
# extras, because generation happens in the API rather than in the console.
adverse-action-engine[ml] @ git+https://github.com/AkanshuAich/adverse-action-engine@main
streamlit>=1.40
