# The console reads the audit chain and renders it. It loads no model, calls no
# language model, and imports neither pandas nor numpy - verified by importing
# aae.console.app and checking sys.modules, after an earlier version of this
# file installed the ml extra on the strength of a comment claiming otherwise.
# That pulled xgboost, shap, mlflow and fairlearn into a deployment that uses
# none of them.
adverse-action-engine @ git+https://github.com/AkanshuAich/adverse-action-engine@main
streamlit>=1.40
